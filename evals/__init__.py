"""Conversation-quality evaluations for the Wealth assistant.

Deterministic checks (``evals.checks``) run offline and in pytest. Model-backed
runs and the LLM judge are invoked only through ``python -m evals.run``.
"""
