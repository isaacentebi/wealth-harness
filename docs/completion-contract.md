# Integration contract

Financial modules expose `run(task, inputs, context)` and return `status`, `result`, `missing`, `warnings`, `sources`, and `assumptions`. The service owns persistence and provenance.

See [DESIGN.md](../DESIGN.md) and [the task catalog](../wealth/catalog.py). The development assignment is [archived](archive/foundation-completion-contract.md).
